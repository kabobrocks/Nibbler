<!DOCTYPE html>
<html>
    <head>
        <title>All Nibbles - Nibbler</title>
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" type="text/css" />
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js" type="text/javascript"></script>
        <script src="{{ asset('js/main.js') }}" type="text/javascript"></script>
        <link href="{{ asset('css/main.css') }}" rel="stylesheet" type="text/css" />
    </head>
    <body>
        <div class="navbar navbar-default navbar-static-top">
            <div class="container">
                <div class="navbar-collapse navbar-collapse-1 collapse" aria-expanded="true">
                    <ul class="nav navbar-nav">
                        <li>
                            <a href="/account"><span class="glyphicon glyphicon-home"></span> Home</a>
                        </li>
                        <li class="active">
                            <a href="/all"><span class="glyphicon glyphicon-bullhorn"></span> All Nibbles</a>
                        </li>
                        <li>
                            <a href="/logout"><span class="glyphicon glyphicon-log-out"></span> Logout</a>
                        </li>
                    </ul>
                    <div class="navbar-form navbar-right">
                        <button onclick="toggleBox();" class="btn btn-warning" type="submit" aria-label="Left Align">
                            <span class="glyphicon glyphicon-pencil" aria-hidden="true"> </span> Nibble
                        </button>

                        <div id="box" class="well clearfix">
                            <form>
                                <textarea id="chars" name="chars" maxlength="139" onkeydown="countChars(this);" class="form-control"></textarea><br />
                                <span id="count">0</span>
                                <button id="nibble" class="btn btn-warning pull-right">Nibble</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="container">
            <div class="row">
                <div class="col-sm-6">
                    <div class="panel panel-info">
                        <div class="panel-body nibbles">
                            {% for post in nibbles %}
                                <div class="media">
                                    <a class="media-left" href="#post">
                                        <img alt="" class="media-object img-rounded" src={{ post.imageURL }}>
                                    </a>
                                    <div class="media-body">
                                        <h4 class="media-heading">@{{ post.handle }}</h4>
                                        <p>{{ post.post }}</p>
                                        <h6>{{ post.stamp|date("M d Y") }}</h6>
                                    </div>
                                </div>
                            {% endfor %}
                        </div>
                    </div>

                    <br>
                </div>
            </div>
        </div>
        {% block content %}{% endblock %}
    </body>
</html>
